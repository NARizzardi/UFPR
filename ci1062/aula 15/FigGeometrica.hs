module FigGeometrica where
    quadrado :: Int -> Int
    retangulo :: Int -> Int -> Int
    triangulo :: Float -> Float -> Float
    
    quadrado l = l * l
    retangulo b a = b * a
    triangulo b a = (b * a) / 2